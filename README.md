# INCEIF iFIKR

Demo UI for the IFIKR assistant experience (React, TypeScript, Vite).

## Development

```bash
npm install
npm run dev
```

## GitHub Pages

Pushes to `main` deploy via `.github/workflows/deploy-pages.yml`. In the repository **Settings → Pages**, set the source to **GitHub Actions**.
